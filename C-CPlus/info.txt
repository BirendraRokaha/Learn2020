Useful terminal C commands


Compile a C program:
gcc [program_name] -o [output name]


Data types and format specifiers:

int %d
char %c
float %f only 2 places %.2f
double %lf

check if string is empty
if (strlen(string) == 0) 
